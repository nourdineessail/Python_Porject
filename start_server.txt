[*] Install Django (Python 3.7) :
        pip install django 
            or
        pip3 install django

[*] Check if its installed :
    python -m django --version
        or
    python3 -m django --version
    
[*] goto to the directory (Log_Off) :
    cd Log_Off [if ure dumb let me be ur teacher ;)]

[*] Start Sever :
    python manage.py runserver
        or
    python3 manage.py runserver